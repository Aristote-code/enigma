---
title: Kbd
description: Display a keyboard key or shortcut.
component: true
---

<ComponentPreview name="kbd-demo" peekCode wide />

## Usage

```tsx
import { Kbd } from 'ui'
```

```tsx
<Kbd>⌘</Kbd>
```

## Examples

### Variants

Kbd supports two sizes via the `size` prop: `sm` and `md` (default). Use `sm` for inline hints within dense text and `md` for standalone shortcut indicators.

<ComponentPreview name="kbd-variants" peekCode />
